// Core imports
import React from "react";

// Fetching and string the necessary images beforehand to save render time
export const ContextPlotImages = React.createContext();
export const ContextPlotData = React.createContext();
